SCHEMA = {
    "incidents": "id, title, severity, status",
    "signals": "id, type, score, source",
    "campaigns": "id, name, status",
    "recipients": "email, risk_score, status",
    "soc_queue": "queue_id, priority, assigned_team, status",
}
